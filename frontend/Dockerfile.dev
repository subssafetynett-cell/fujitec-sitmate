# Local / overlay: Vite HMR for development
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* bun.lock* ./
RUN npm install --include=dev
COPY . .
ENV NODE_ENV=development
EXPOSE 8080
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "8080"]
